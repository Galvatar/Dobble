interface Player {
    name: string,
    score: number,
    connected: boolean,
    host: boolean,
}

interface Card {

}

interface Symbol {
    id: number,
    bottom: number,
    left: number,
    size: number,
    angle: number
}